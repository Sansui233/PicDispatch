<Window x:Class="PicDispatch.ShortcutInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Shortcut"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="12"
                            GlassFrameThickness="1"
                            ResizeBorderThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource AppBackgroundBrush}" CornerRadius="12" ClipToBounds="True">
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="绑定快捷键" FontSize="20" FontWeight="SemiBold"/>
            <StackPanel Grid.Row="1" Margin="0,14,0,18">
                <TextBlock Text="{Binding Prompt}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                <Border Margin="0,16,0,0" Background="{StaticResource KeyBadgeBackgroundBrush}" CornerRadius="10" Padding="18,12" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding ShortcutText}" FontSize="24" FontWeight="SemiBold"/>
                </Border>
                <TextBlock Text="{Binding ValidationMessage}" Foreground="{StaticResource DangerBrush}" Margin="0,12,0,0"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="Cancel_Click" Style="{StaticResource TextButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="Add" Click="Add_Click" Style="{StaticResource TextButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
